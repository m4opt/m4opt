import AbstractConstraints

"""
These are all gonna look pretty damn similar as the difference lies within the
implementation, not the setup
"""

class TimeLimitConstraint(AbstractConstraints.GeneralConstraint):

    """
    Instantiate this constraint
    """
    def __init__(self, observerData: dict, constraintParams: dict):
        """
        Constraint-specific info goes here
        We want ID, and then any specific info needed for this constraint
        """


    """
    Add this constraint to the model
    """

    def add_self_to_model(self, model: object) -> None:


    """
    Remove this constraint from the model
    """

    def remove_self_from_model(self, model: object) -> None:


    """
    Define our string method so you can easily represent this in a print
    statement
    """
    def __str__(self) -> str:


class OverheadTimeConstraint(AbstractConstraints.GeneralConstraint):
    """
    Instantiate this constraint
    """

    def __init__(self, observerData: dict, constraintParams: dict):
        """
        Constraint-specific info goes here
        We want ID, and then any specific info needed for this constraint
        """

    """
    Add this constraint to the model
    """

    def add_self_to_model(self, model: object) -> None:

    """
    Remove this constraint from the model
    """

    def remove_self_from_model(self, model: object) -> None:



    """
    Define our string method so you can easily represent this in a print
    statement
    """

    def __str__(self) -> str:

class DeadTimeConstraint(AbstractConstraints.GeneralConstraint):
    """
    Instantiate this constraint
    """

    def __init__(self, observerData: dict, constraintParams: dict):
        """
        Constraint-specific info goes here
        We want ID, and then any specific info needed for this constraint
        """

    """
    Add this constraint to the model
    """

    def add_self_to_model(self, model: object) -> None:

    """
    Remove this constraint from the model
    """

    def remove_self_from_model(self, model: object) -> None:

    """
    Define our string method so you can easily represent this in a print
    statement
    """

    def __str__(self) -> str:

class SolarElevationAngleConstraint(AbstractConstraints.GeneralConstraint):
    """
    Instantiate this constraint
    """

    def __init__(self, observerData: dict, constraintParams: dict):
        """
        Constraint-specific info goes here
        We want ID, and then any specific info needed for this constraint
        """

    """
    Add this constraint to the model
    """

    def add_self_to_model(self, model: object) -> None:

    """
    Remove this constraint from the model
    """

    def remove_self_from_model(self, model: object) -> None:

    """
    Define our string method so you can easily represent this in a print
    statement
    """

    def __str__(self) -> str:

class CadenceConstraint(AbstractConstraints.GeneralConstraint):
    """
    Instantiate this constraint
    """

    def __init__(self, observerData: dict, constraintParams: dict):
        """
        Constraint-specific info goes here
        We want ID, and then any specific info needed for this constraint
        """

    """
    Add this constraint to the model
    """

    def add_self_to_model(self, model: object) -> None:

    """
    Remove this constraint from the model
    """

    def remove_self_from_model(self, model: object) -> None:

    """
    Define our string method so you can easily represent this in a print
    statement
    """

    def __str__(self) -> str:


class TimeDeadlineConstraint(AbstractConstraints.GeneralConstraint):
    """
    Instantiate this constraint
    """

    def __init__(self, observerData: dict, constraintParams: dict):
        """
        Constraint-specific info goes here
        We want ID, and then any specific info needed for this constraint
        """

    """
    Add this constraint to the model
    """

    def add_self_to_model(self, model: object) -> None:

    """
    Remove this constraint from the model
    """

    def remove_self_from_model(self, model: object) -> None:

    """
    Define our string method so you can easily represent this in a print
    statement
    """

    def __str__(self) -> str:

class MoonIlluminationConstraint(AbstractConstraints.GeneralConstraint):
    """
    Instantiate this constraint
    """

    def __init__(self, observerData: dict, constraintParams: dict):
        """
        Constraint-specific info goes here
        We want ID, and then any specific info needed for this constraint
        """

    """
    Add this constraint to the model
    """

    def add_self_to_model(self, model: object) -> None:

    """
    Remove this constraint from the model
    """

    def remove_self_from_model(self, model: object) -> None:

    """
    Define our string method so you can easily represent this in a print
    statement
    """

    def __str__(self) -> str:

